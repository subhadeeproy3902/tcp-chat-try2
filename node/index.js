// This file serves as the entry point for the Render deployment
// It simply requires and runs the TCP server

// Import the TCP server
require('./tcp-server');

console.log('Server started via index.js entry point');
